package game.enums;

/**
 * Enum to represent the items capabilities.
 */
public enum ItemCapability {
  UNLOCK_GATE,
  HEAL,
  STAMINA_BOOST,
  RUNES_BOOST,
  PURCHASABLE,
  SELLABLE,
  UPGRADABLE,
  RESETTABLE,
}
